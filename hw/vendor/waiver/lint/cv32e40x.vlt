`verilator_config

lint_off -rule DECLFILENAME -file "*/bhv/cv32e40x_sim_clock_gate.sv" -match "Filename 'cv32e40x_sim_clock_gate' does not match MODULE name: 'cv32e40x_clock_gate'"
lint_off -rule WIDTH -file "*/rtl/cv32e40x_ff_one.sv" -match "Operator ASSIGNW expects 5 bits on the Assign RHS, but Assign RHS's UNSIGNED generates 32 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 3 bits on the RHS, but RHS's VARREF 'pop_q' generates 1 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 3 bits on the RHS, but RHS's VARREF 'pop_q' generates 1 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator NEQ expects 3 bits on the LHS, but LHS's VARREF 'outstanding_cnt_q' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'wptr' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr2' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr2' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr2' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator SUB expects 32 or 6 bits on the RHS, but RHS's VARREF 'rptr2' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'wptr_req_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'wptr_resp_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'wptr_req_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'wptr_resp_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q_inc' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi_instr_obi.sv" -match "Operator SUB expects 32 or 7 bits on the RHS, but RHS's VARREF 'rptr_q_inc' generates 2 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi.sv" -match "Operator ASSIGNDLY expects 14 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi.sv" -match "Operator ASSIGNDLY expects 14 bits on the Assign RHS, but Assign RHS's CONST '1'h0' generates 1 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator COND expects 3 bits on the Conditional True, but Conditional True's CONST '2'h1' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator COND expects 3 bits on the Conditional False, but Conditional False's CONST '2'h0' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alignment_buffer.sv" -match "Operator ASSIGNDLY expects 105 bits on the Assign RHS, but Assign RHS's VARREF 'INST_RESP_RESET_VAL' generates 35 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_controller_fsm.sv" -match "Operator ASSIGN expects 32 bits on the Assign RHS, but Assign RHS's ENUMITEMREF 'PC_WB' generates 2 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_alu.sv" -match "Operator COND expects 6 bits on the Conditional False, but Conditional False's VARREF 'ff1_result' generates 5 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_div.sv" -match "Operator NOT expects 33 bits on the LHS, but LHS's VARREF 'op_b_i' generates 32 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_compressed_decoder.sv" -match "Operator ASSIGN expects 32 bits on the Assign RHS, but Assign RHS's REPLICATE generates 31 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_compressed_decoder.sv" -match "Operator ASSIGN expects 32 bits on the Assign RHS, but Assign RHS's REPLICATE generates 31 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_int_controller.sv" -match "Operator ASSIGN expects 5 bits on the Assign RHS, but Assign RHS's VARREF 'CSR_MEIX_BIT' generates 32 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_int_controller.sv" -match "Operator ASSIGN expects 5 bits on the Assign RHS, but Assign RHS's VARREF 'CSR_MSIX_BIT' generates 32 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_int_controller.sv" -match "Operator ASSIGN expects 5 bits on the Assign RHS, but Assign RHS's VARREF 'CSR_MTIX_BIT' generates 32 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_if_stage.sv" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'xif_id' generates 4 bits."
lint_off -rule WIDTH -file "*/rtl/cv32e40x_if_stage.sv" -match "Operator ASSIGNDLY expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'xif_id' generates 4 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi.sv" -match "Logical operator IF expects 1 bit on the If, but If's SEL generates 32 bits."
lint_off -rule WIDTH -file "*/bhv/cv32e40x_rvfi.sv" -match "Operator ASSIGNW expects 32 bits on the Assign RHS, but Assign RHS's VARREF 'csr_tinfo_we_i' generates 1 bits."
lint_off -rule UNDRIVEN -file "*/rtl/if_xif.sv" -match "*"
lint_off -rule UNDRIVEN -file "*/rtl/cv32e40x_id_stage.sv" -match "*"
lint_off -rule UNUSED -file "*cv32e40x_*.sv" -match "*"
lint_off -rule UNUSED -file "*if_xif*.sv" -match "*"
lint_off -rule UNUSED -file "*cv32e40p_*.sv" -match "*"
lint_off -rule UNDRIVEN -file "*cpu_subsystem.sv" -match "*"
lint_off -rule UNUSED -file "*cpu_subsystem*.sv" -match "*"
lint_off -rule LITENDIAN -file "*cv32e40x_alignment_buffer*.sv" -match "*"
lint_off -rule LITENDIAN -file "*cv32e40x_rvfi_instr_obi*.sv" -match "*"
lint_off -rule CASEINCOMPLETE -file "*/rtl/cv32e40x_*.sv" -match "Case values*"
lint_off -rule UNOPTFLAT -file "*/rtl/cv32e40x_*.sv" -match "Signal unoptimizable: Feedback to clock or circular logic: '*'"
lint_off -rule IMPERFECTSCH -file "*/rtl/cv32e40x_*.sv" -match "Imperfect scheduling of variable: '*'"
